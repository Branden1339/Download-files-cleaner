Use os package to iterate through downloads directory
Input file beginning and end
Choose to move, add to folder, or delete file
Uses shutil package to move file
Uses send2trash package to delete file
